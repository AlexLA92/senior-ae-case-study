version: 2

models:
  - name: stg_sites
    description: Staged sites data.
    columns:
      - name: site_id
        description: Unique identifier for the site.
      - name: site_name
        description: Name of the research site.
      - name: country_code
        description: ISO country code.
      - name: country_name
        description: Full country name.
