version: 2

models:
  - name: dim_sites
    description: Site dimension table.
    columns:
      - name: site_id
        description: Primary key.
      - name: country_code
        description: ISO country code.
      - name: country_name
        description: Full country name.
